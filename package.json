{
  "name": "@oajarvis/gh-pkg-consumer-test",
  "version": "1.0.0",
  "description": "Testing consumption of npm package",
  "author": "Orry Jarvis <oajarvis@sep.com>",
  "scripts": {
    "dev": "ts-node-dev --poll --no-notify --respawn --transpile-only src/index",
    "start": "node dist/index",
    "test": "jest",
    "clean": "rm -rf dist",
    "build": "npm -s run clean && tsc"
  },
  "files": [
    "lib/**/*"
  ],
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.2.4",
    "jest": "^29.3.1",
    "ts-jest": "^29.0.3",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "@oajarvis/npm-test": "1.0.2"
  }
}